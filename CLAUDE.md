# OSIRIS Situational Dashboard, Claude notes

Static-first OSINT situational-awareness dashboard. See `README.md` for the full feature list and run instructions.

## Architecture (quick map)

- `server.js`: Node HTTP server (no framework, no npm deps). Serves `public/` and handles `/api/*`. Binds `HOST` (default `127.0.0.1`; the container and compose set `0.0.0.0`) on `PORT` (default 4173).
- `public/`: vanilla-JS frontend (`app.js`, `data.js`, `index.html`, `styles.css`). MapLibre GL and deck.gl load from CDN in the browser. Client features: localStorage persistence (layers/viewport/recon history/min-severity), zoom-aware grid clustering + severity filter, per-layer polling, JSON snapshot export.
- `public/data/*.json`: versioned static datasets with provenance (`gazetteer`, `chokepoints`, `cctv`, `conflict`). The browser fetches the layer datasets; `src/lib/gazetteer.js` reads `gazetteer.json` via `readFileSync`.
- `src/adapters/*`: one module per intelligence layer; each normalizes an upstream feed to `{ entities, meta }`. `intel.js` also has the keyless RDAP `whoisLookup` + `sanctionsCrossCheck`.
- `src/lib/*`: `cache` (in-memory TTL Map), `health` (source telemetry), `http` (fetch helpers), `normalize`, `gazetteer` (geoparse + confidence for text feeds).
- No database. Cache and health are in-memory and reset on restart.

## Run

`npm start`, then open http://localhost:4173. API keys are optional (see `.env.example`); each layer falls back to keyless or static behavior without its key.

## Deploy

Packaged for a LAN-only single-container Docker Compose homelab deploy: `Dockerfile`, `docker-compose.prod.yml`, and `docs/DEPLOY.md`. NOT yet deployed or promoted, still active dev in `01_Projects`. When feature-stable, relocate to `06_Production_Apps/Homelab_Sever/` and deploy per `docs/DEPLOY.md`.

Keep it LAN-only: the `/api/intel/*` and `/api/crypto/*` routes proxy external services using your API keys with no auth or rate limiting. Do not expose to the internet without adding auth and rate limiting first.

## Status / Next

Active development. 7 of 8 README build phases done (2026-07-17): gazetteer + confidence (1), clustering + severity filters (2), per-layer polling (3), localStorage persistence (4), WHOIS/RDAP + OpenSanctions cross-check (5), versioned JSON datasets (6), snapshot export (8). All uncommitted on `main`.

Remaining / next:
- README phase 7: authenticated adapters for additional higher-quota sources (open-ended; needs real API keys). `.env.example` documents all optional keys (FREE/PAID tagged).
- Dead data to resolve: `staticLayers` no longer holds the unused `ports`/`news`/`space` arrays (they had no `staticKey`); confirm they should stay dropped.
- Still NOT promoted/deployed: when feature-stable, relocate to `06_Production_Apps/Homelab_Sever/` and deploy per `docs/DEPLOY.md`.

Uncommitted changes this session (2026-07-17), verified but not yet committed:
- Fixed a listener leak in `renderLayerControls` (`public/app.js`) — the per-render `{ once:true }` `change` listener accumulated and fired duplicate upstream fetches on every toggle (8x measured). Now one delegated listener wired once via `wireLayerControls()`.
- `military` layer (no adapter) renders disabled/dimmed as "(soon)" instead of silently doing nothing (`public/app.js`, `public/styles.css`).
- Added inline SVG favicon (`public/index.html`) — removes the 404.
- Slack alerting on source failure/recovery: `src/lib/notify.js` (new), wired into `withHealth` (`src/lib/health.js`); `src/lib/http.js` attaches `error.status` so 429/403 are detectable. Config: `SLACK_WEBHOOK_URL`, `ALERT_COOLDOWN_MS` (see `.env.example`, `docs/DEPLOY.md`).
- Added a test suite: `npm test` (`node --test test/*.test.js`, zero deps). 72 tests in `test/`, ~74% line coverage overall (measure with `node --test --experimental-test-coverage test/*.test.js`):
  - lib: `normalize`, `cache`, `gazetteer` (geoparse word-boundary/confidence), `health`, `notify` (alerting: rate-limit tagging, cooldown, recovery), `http` (status attachment) — all ~100%.
  - adapters (via `test/helpers/mock-fetch.js` — `installJsonFetch`/`installFetch` stub global `fetch` + clear cache): `usgs`/`ports`/`eonet`/`opensky` normalization; `recon` (85%: OFAC crypto-address XML parse, BTC/ETH sanctioned flag, cryptoLayer, sanctionsLayer grouped+individual, sanctionsSearch fallback, cveSearch); `intel` (61%: whoisLookup domain+IP RDAP parse, sanctionsCrossCheck name filtering + name-level match).
  - `server.js` routing (73%): static serving, traversal block, `/api/health`, `/api/layers/:layer`, `/api/crypto/btc`, `/api/sanctions`, `/api/cves`, `/api/intel/whois`, and 400/404 error paths.
- Minor `server.js` refactor for testability: exports `createServer()` and only calls `.listen()` when run directly (`node server.js`), so tests import the handler without binding a port. No behavior change to `npm start`.
- Frontend UI additions (`public/app.js`, `index.html`, `styles.css`): Clear-all button in the Layers panel; collapsible left (layers) and right (recon) panes via `‹`/`›` topbar toggles (state persisted in `store.hideLeft`/`hideRight`, map resized on toggle, desktop-only via `@media (min-width:1081px)`); detail cards now render fields as differentiated `dt`/`dd` rows (muted uppercase label + bright value) instead of a wall of identical `<p>` lines. Verified live via Playwright.
- Custom map + menu icons for the 5 layers that previously fell through to plain scatter dots: chokepoints (inward funnels), CCTV (camera), news (broadcast waves), conflict (crossed swords), maritime (ship) — each added as an SVG icon atlas + mapping + `menuIcons` entry + a `buildIconLayer` branch in `public/app.js`.
- Military coverage is now TWO layers (user chose "alongside"):
  - `military` — static "Military Bases": `public/data/military.json` (22 publicly-documented installations), `staticKey:"military"`, slate 5-point-star icon.
  - `military-air` — LIVE "Military Aircraft": `militaryAircraftLayer()` in `src/adapters/opensky.js` filters the shared OpenSky state feed to military ICAO24 hex ranges (`MILITARY_HEX_RANGES`, the 32-range `.military` list from tar1090-db `ranges.json`), amber plane icon, dead-reckoned. Wired via `layers.js` + `data.js` (`live:true`). `opensky.js` was refactored to share `openSkyStates()`/`stateEntity()` between aviation and military (one upstream call + 30s cache serves both). Heuristic caveat: coverage depends on OpenSky data + the hex list; civilian-squawking or ADS-B-off military won't show. Verified live: 70 military aircraft worldwide, correct hex filtering.
- Upstream resilience (`src/lib/cache.js`, `src/lib/http.js`, adapters): `cachedResilient()` cache variant de-duplicates concurrent loads for one key (a burst of viewport refreshes → one upstream call) and serves the last-good (even expired) value with a `stale` flag when the loader fails instead of throwing; shared `fetchJsonRetry()`/`fetchTextRetry()` in `http.js` add a per-attempt `AbortSignal.timeout` (so a *stall* fails fast — plain fetch had no timeout and hung ~2 min) + one retry on 5xx/network/timeout (never on 4xx, so a 429 isn't hammered). Only a cold failure with nothing cached still errors. Now applied across **all HTTP-based cached upstreams**: ports (NGA WPI), USGS, EONET, OFAC/UN/UK sanctions XML + NVD (recon.js), and — extended app-wide — FIRMS, NOAA space/N2YO, cyber (NVD/CISA KEV/EPSS), NewsAPI, OpenSky states (aviation+military share one key → dedup halves calls), Telegram previews, geocode (Nominatim), and intel (AbuseIPDB, VirusTotal ×3, RDAP whois). GreyNoise got a bespoke tweak (timeout + `withRetry`, keeping its 404-as-valid handling); `withRetry` is now exported from http.js. NOT touched (deliberately): maritime (WebSocket, not HTTP) and the OpenSky OAuth token POST.
  - **Staleness surfaced in the UI**: each layer adapter now threads `meta.stale` (true when serving cached data because the upstream failed). `renderLayerControls` generalized the old `needs-key` flag into a single `flagged` state (CSS `.layer-row.flagged` / `.flag-mark`) covering BOTH keyless/fallback (`meta.configured === false`) and stale (`meta.stale === true`), each with its own amber ⚠ + tooltip — so "resilient" never means "silently showing old data". Verified live (all swapped endpoints return data + `stale:false`) and in-browser (FIRMS needs-key ⚠ + injected-stale ⚠ both render with correct tooltips). Unit tests cover retry (5xx yes / 4xx no), stale-serve, dedup, cold-throw.
- Fixed the Feeds "Live Desk" feed (`public/app.js` `renderFeeds`): it was hardcoded to pull only telegram/conflict/seismic from `state.data` regardless of enabled state — so it ignored every other enabled layer (aviation, cyber, sanctions, …) AND still showed items from toggled-OFF layers. Now it aggregates `visibleEntities` across all ENABLED layers (respects the severity filter), caps each layer's contribution (`FEED_PER_LAYER=4`) so one noisy layer (e.g. an earthquake swarm) can't flood it, then sorts by severity→recency and takes the top `FEED_LIMIT=14`. Each item is tagged with its layer label. Verified: with conflict/seismic/cyber/aviation enabled the feed shows all four (Ukraine/Gaza surface), disabling telegram removes it, and clicking an item still flies + opens its detail card. Source-health half was already correct.
- Fixed recon tab-bar layout (`styles.css`): the `.tabs` grid was `repeat(4,1fr)` with 5 tabs (Crypto/Sanctions/Intel/Cyber/Feeds), so Feeds wrapped to a misaligned second row. Now `repeat(5,1fr)` (with smaller tab font/padding) → all 5 on one row. The tabs themselves always worked functionally.
- Recon-panel results are now interactive (`public/app.js`, `styles.css`):
  - Sanctions (SDN) hits are click-to-expand accordions revealing hidden OpenSanctions/OFAC properties (country, programs, aliases, OFAC UID, notes, source) via `sanctionDetail()`.
  - CVE hits are accordions too (`cveDetail()`): CVSS score/severity, publish date, full description, an "Open on NVD" link, and reference links.
  - Wallet lookups add a block-explorer link (Blockstream for BTC, Etherscan for ETH); IOC/IP lookups add pivot links (VirusTotal, AbuseIPDB, GreyNoise; domain adds AlienVault OTX).
  - Shared helpers: `wireAccordion()` (toggle listeners, no accumulation), `kvRow()`, `extLink()`.
  - Gotcha fixed: `.result-detail { display:flex }` overrode the `hidden` attribute, so a `.result-detail[hidden]{display:none}` rule was required.
- Two layer-list UX tweaks (`public/app.js`, `styles.css`): (1) static layers now show their dataset size in the count badge even when toggled OFF (Military Bases 22, CCTV 10, etc.) instead of 0 — reads `staticLayers[staticKey].length` when not loaded; (2) a live layer whose adapter reports `meta.configured === false` (e.g. FIRMS with no `FIRMS_MAP_KEY` returns 0) gets an amber ⚠ flag + amber count + a `title` tooltip (`meta.message`), so an empty count isn't mistaken for "no data". Layer meta is now captured in `state.meta` by `fetchLayer`.
- Two frontend bug fixes (`public/app.js`, `styles.css`):
  - Place search (Enter in the top bar) only matched already-loaded entities and silently no-op'd otherwise. Now a 3-tier search: (1) loaded entities, (2) gazetteer (`loadGazetteer()` fetches `/data/gazetteer.json` client-side), (3) full geocoding of arbitrary street addresses via a new keyless Nominatim/OpenStreetMap proxy — `src/adapters/geo.js` `geocode()` (24h cached) behind `GET /api/geocode?q=`. On a hit the map flies to the point (zoom 14) and shows a "Location" detail card; only a true miss triggers the `.no-match` shake. Verified: "Tokyo" → gazetteer; "350 Fifth Avenue, New York" → geocoded to 40.748,-73.986 at street zoom.
  - Aircraft looked frozen: they DO auto-refresh (~60s poll, confirmed) but per-refresh movement is sub-pixel at map scale and there's no motion between snapshots. Added client-side dead-reckoning (`deadReckonAircraft`, 1s tick) that advances each plane from its `velocity`+`track` between refreshes; the server refresh snaps it back to truth. `renderAll` was split so the tick calls a lightweight `renderMap()` (map + counts only, no panel rebuild / no `/api/health` fetch). Verified numerically: a 246 m/s plane moved 985 m in 4 s (expected 984).

## Known issues / future enhancements

> The top-3 items below (frontend tests, ETH lookup, ports cold-start) were RESOLVED on 2026-07-18 — see the "Resolved 2026-07-18" note at the end of this section. `docs/NEXT-SESSION-PLAN.md` has the original plan + a post-mortem.

- ~~**ETH wallet lookup broken**~~ DONE (2026-07-18): `eth.blockscout.com` was dead (TLS/HTTP 000). ETH now uses a two-source `fetchEthData` in `src/adapters/recon.js`: **Ethplorer** (`api.ethplorer.io/getAddressInfo?apiKey=freekey`, keyless demo key) as primary for rich data (balance + up to 25 ERC-20 token holdings), falling back to the keyless public RPC `ethereum.publicnode.com` (`eth_getBalance`, balance-only) with a `note` when Ethplorer throttles/fails. Both `ethLookup` and `btcLookup` degrade gracefully: if the chain fetch fully fails it returns `data: { error }` so the OFAC sanctions verdict is never lost; both use `fetchJsonRetry` (timeout + retry). Caveat: `freekey` is a shared demo key that can rate-limit — the RPC fallback covers that. Verified live: sanctioned=true + 101.8 ETH + 21 tokens on a live OFAC address; RPC-fallback path unit-tested. Blockchair was evaluated but is blocked (HTTP 430).
- ~~`military` layer placeholder~~ DONE: now a static layer (`public/data/military.json`, 22 publicly-documented major installations) wired via `STATIC_DATASETS` + `staticKey: "military"`, with a slate 5-point-star icon.
- ~~**Ports cold-start-while-NGA-down errors**~~ DONE (2026-07-18): bundled `public/data/ports-fallback.json` (417 Large/Medium WPI harbors, public-domain U.S. Gov, ~197 KB). `portsLayer` wraps `cachedResilient` in try/catch and serves the fallback with `meta.stale=true`+`meta.fallback=true` on a cold failure (nothing cached + NGA 503). The `.layer-row.flagged` ⚠ indicator surfaces it automatically. Regenerate the fallback when NGA is up: fetch the WPI, keep `["L","M"].includes(harborSize)`, trim to the fields `portEntity` reads. Verified: 417 ports render on a mocked cold 503.
- **Alerting scope** — Slack alerts fire on ALL source failures (rate-limits tagged 🚫, other errors ⚠️), cooldown-throttled per source. If non-rate-limit noise is unwanted, add a rate-limit-only filter in `src/lib/notify.js`.
- **Optional Docker hardening** — Dockerfile runs as root; add `USER node`.
- **Test coverage gap** — covered (~78% overall, 95 tests): all `src/lib/*`, adapters `usgs`/`ports` (100%)/`eonet`/`opensky`/`recon` (86%)/`intel`, `server.js` routing, and now `public/logic.js` (100%). Still near-zero (funcs 0%): adapters `cyber` (KEV/NVD/EPSS merge), `firms` (CSV parse), `telegram` (HTML geoparse), `space` (NOAA SWPC/N2YO), `maritime`, `news`. Partial remainders: `recon` UN/UK sanctions XML parsers and `intel` VirusTotal/AbuseIPDB/GreyNoise lookups (need keys). The `mock-fetch.js` pattern extends to all; `telegram`/`cyber` are the higher-value next targets.
- **Frontend partially tested** — DONE (2026-07-18): the pure, DOM-free logic was extracted to `public/logic.js` (side-effect-free, Node-importable) and unit-tested in `test/frontend-logic.test.js` (15 tests, 100% line coverage): `escapeHtml`, `clusterPoints`, `shouldCluster`, `filterBySeverity`, `advancePosition`, `byPriority`, `buildFeed`, `detailRows`, `snapshotEntity`, `kvRow`, `extLink`, `sanctionDetail`, `intelLinks`, `cveDetail`. `app.js` imports them back. Still untested (needs a real browser + deck.gl, verified only via Playwright): `initMap`, `renderMap`, `buildIconLayer`, `showDetail`, `renderLayerControls`, map interactions. Do NOT add a browser test-runner dep (zero-dep rule). Manual smoke: toggle layers, cluster/uncluster by zoom, click icon→detail, search an address, expand a sanctions/CVE result, Feeds tab.
- **Deploy prerequisite** — the documented deploy flow (`git clone` / `git pull`) means uncommitted fixes above must be committed + pushed before deploying, or the server pulls stale code.
